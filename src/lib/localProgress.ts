export type Progress={version:1;visitedChurchIds:string[];favoriteChurchIds:string[];foundDiscoverItemIds:string[];quizAnswers:Record<string,{answer:string;isCorrect:boolean}>;badges:string[];churchNotes:Record<string,string>;lastVisitDates:Record<string,string>};const KEY='clochers_progress_v1';const base=():Progress=>({version:1,visitedChurchIds:[],favoriteChurchIds:[],foundDiscoverItemIds:[],quizAnswers:{},badges:[],churchNotes:{},lastVisitDates:{}});const uniq=(v:string[])=>[...new Set(v)];export const getProgress=():Progress=>{try{const raw=localStorage.getItem(KEY);if(!raw)return base();const p=JSON.parse(raw);return {...base(),...p,visitedChurchIds:uniq(p?.visitedChurchIds??[]),favoriteChurchIds:uniq(p?.favoriteChurchIds??[]),foundDiscoverItemIds:uniq(p?.foundDiscoverItemIds??[])};}catch{return base();}};export const saveProgress=(p:Progress)=>localStorage.setItem(KEY,JSON.stringify(p));const update=(fn:(p:Progress)=>Progress)=>{const p=fn(getProgress());saveProgress(p);return p};export const toggleFavorite=(id:string)=>update(p=>({...p,favoriteChurchIds:p.favoriteChurchIds.includes(id)?p.favoriteChurchIds.filter(x=>x!==id):[...p.favoriteChurchIds,id]}));export const markChurchVisited=(id:string)=>update(p=>({...p,visitedChurchIds:uniq([...p.visitedChurchIds,id]),lastVisitDates:{...p.lastVisitDates,[id]:new Date().toISOString()}}));export const markDiscoverItemFound=(cid:string,iid:string)=>update(p=>({...p,foundDiscoverItemIds:uniq([...p.foundDiscoverItemIds,`${cid}:${iid}`])}));export const saveQuizAnswer=(cid:string,iid:string,answer:string,isCorrect:boolean)=>update(p=>({...p,quizAnswers:{...p.quizAnswers,[`${cid}:${iid}`]:{answer,isCorrect}}}));export const saveChurchNote=(id:string,note:string)=>update(p=>({...p,churchNotes:{...p.churchNotes,[id]:note}}));export const resetProgress=()=>{localStorage.removeItem(KEY);return base();};
